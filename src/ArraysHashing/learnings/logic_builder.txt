217.
   1.Duplicates - Sorting Helps
   2.HashMap when index needed , hashSet when only value matters

242.
   1.Arrays.equals(sArr, tArr);
   2.String s1 = new String(sArr);
           String s2 = new String(tArr);
           for(int i=0;i<s1.length();i++){
              if(s1.charAt(i)!=s2.charAt(i)) return false;
           }
           return true;
   1 replcaes 2 [ google notices verbosity ]

   
   3.freqMap.values().stream().anyMatch(v -> v != 0);
   4..for(Map.Entry<Character,Integer> entry : freqMap.entrySet()){
           if( entry.getValue()!=0) return false;
     }

   3 replaces 4 [ google prefers depends operation like hasNegative n->n<0 ] 

   5.return new ArrayList<>(groupWords.values());  // one line!
   6.List<List<String>> result = new ArrayList<List<String>>();
     for(Map.Entry<String,List<String>> group : groupWords.entrySet()){
       result.add(group.getValue());
     }

   5 replaces 6 if we want only values we can use map.getValues()

   7.char[] freq = new char[26];
     new String(freqKey); // internally it creates unique values like \u0002 \u0004  0  0  \u0001
